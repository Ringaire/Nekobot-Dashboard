import { memo } from 'react';

// ==============================|| SIDEBAR - MENU CARD ||============================== //

function MenuCard() {
  // 已移除存储空间推广卡片
  return null;
}

export default memo(MenuCard);
